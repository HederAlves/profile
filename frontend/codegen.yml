overwrite: true
schema: "http://localhost:4000"
documents: "src/graphql/queries/Users.ts"
generates:
  src/gql/:
    preset: "client"
    plugins: []
  ./graphql.schema.json:
    plugins:
      - "introspection"
